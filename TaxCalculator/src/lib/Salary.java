package lib;

public class Salary {

    private static final boolean isForeigner = false;
    private int monthlySalary;
	private int otherMonthlyIncome;
	private int annualDeductible;

    /**
	 * Dari perhitungan ini bisa dilihat bahwa kit menghitung pendapatan seorang pegawai dengan perhitungan perbulan
	 * Dan dapat disimpulkan warga negara asing memiliki gaji bulanan diperbesar sebanyak 50%
	 */
	
	public void setMonthlySalary(int grade) {	
		if (grade == 1) {
			monthlySalary = 3000000;
			if (isForeigner) {
				monthlySalary = (int) (3000000 * 1.5);
			}
		}else if (grade == 2) {
			monthlySalary = 5000000;
			if (isForeigner) {
				monthlySalary = (int) (3000000 * 1.5);
			}
		}else if (grade == 3) {
			monthlySalary = 7000000;
			if (isForeigner) {
				monthlySalary = (int) (3000000 * 1.5);
			}
		}
	}
	
	public void setAnnualDeductible(int deductible) {	
		this.annualDeductible = deductible;
	}
	
	public void setAdditionalIncome(int income) {	
		this.otherMonthlyIncome = income;
	}
	
}